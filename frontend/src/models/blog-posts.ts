export interface BlogPost {
    _id: string,
    slug: string,
    title: string,
    summary: string,
    body: string,
    feauturedImageUrl: string,
    createdAt: string,
    updatedAt: string,
}
